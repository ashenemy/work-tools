import { InternalOptions } from '../@types';

export const DEFAULT_OPTIONS: InternalOptions = {
    blockConsoleOutput: true,
    autoAttachFactoryQueues: true,
    renderThrottleMs: 40,
    maxTaskLines: 300,
    maxErrorLines: 300,
};
