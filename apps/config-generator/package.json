{
    "name": "@work-tools/config-cenerator",
    "version": "0.0.1",
    "private": true,
    "nx": {
        "targets": {
            "build": {
                "executor": "nx:run-commands",
                "options": {
                    "cwd": "apps/config-generator",
                    "command": "ts-node src/main.ts"
                },
                "dependsOn": [
                    "typings"
                ]
            },
            "typings": {
                "executor": "nx:run-commands",
                "options": {
                    "cwd": "apps/config-generator",
                    "command": "npx tsc \"src/config/config.toml.ts\" --declaration --emitDeclarationOnly --declarationDir ./build"
                }
            },
            "check": {
                "executor": "@work-tools/nx-biome:check"
            },
            "check-fix": {
                "executor": "@work-tools/nx-biome:check",
                "options": {
                    "fix": true
                }
            }
        }
    },
    "main": "./build/config.toml",
    "types": "./build/config.toml.d.ts"
}
